# useForm

Example of use:

```
const initialForm = {
  name: '',
  age: 0,
  email: '',
}
 const [values, handleInputChange, reset] = useForm(initialForm);
```

useForm() // has an initialState = {}
